Python script that parses a selected list of metadata and searches for this metadata via the Library of Congress's catalog via the SRU (search and retrieve via URL) protocol. Tracks MARCXML files that have already been downloaded from SRU in a corresponding directory.
